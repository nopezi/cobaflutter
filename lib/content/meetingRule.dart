import 'package:flutter/material.dart';

class MeetingRule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        backgroundColor: Colors.lightBlueAccent,
        centerTitle: true,
        title: Text('Meeting Rule'),
      ),
    );
  }
}